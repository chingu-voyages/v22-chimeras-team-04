import { handleAuthClick } from './gmailConnector'
let loginLink = document.getElementById('loginLink')
// TODO: 1. add to loginlink on clicklistener which calls handleAuthClick
// TODO: 2. Upon successful login move user to topTable.html

// loginLink.addEventListener("click", handleAuthClick);